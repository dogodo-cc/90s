// const computedBehavior = require('miniprogram-computed');
Component({
  // behaviors: [computedBehavior],
  externalClasses: ['m-icon'],
  properties: {
    icon: {
      type: String,
      value: ""
    },
    color: {
      type: String,
      value: "#fff"
    },
    fontSize: {
      type: String,
      value: "40rpx"
    }
  }
})
