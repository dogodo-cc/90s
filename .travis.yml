language: node_js
node_js: '10'
branches:
  only: master
cache:
  directories: node_modules
install: npm install
script: npm run build

after_success:
  cp ./CNAME ./dist 
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: gh-pages
  local_dir: "./dist"
  on:
    branch: master
